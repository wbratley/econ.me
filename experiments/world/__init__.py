"""The "world" content-pack experiment (Phase 0 of the to-market game,
``docs/game.md`` §12).

A richer single-region world -- a multi-stage industrial chain gated by a
tech tree, plus a food chain -- authored entirely as DATA + Lua on top of an
untouched engine. This is the substrate every later phase (player control,
governance cadence, victory observer) is built on.

See ``README.md`` for the design; ``scenario.py`` for the genesis; ``run.py``
for the CLI harness; ``test_world.py`` for the proving tests.
"""
